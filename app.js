auth.dataset.mode = next;
// the blade covers the card for a fifth
panes[next].classList.add('is-on');
// of its travel, and the two states

later(() => {
  // trade places under that dark
  panes[other].inert = true;
  panes[next].inert = false;
}, T.cover);
